import React from 'react';

import {ModalProps} from 'util/ModalProvider';

const GraphChangeTypeModal = () => (
    <>
        <ModalProps
            title='Change Visual Type'
        />
    </>
);

export default GraphChangeTypeModal;
